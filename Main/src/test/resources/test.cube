import io;
import random;
import time;

fun fib(n: i32): i32{
    if(n < 2) {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}
struct x {
    var i: i32;
    var j: i32;
    var k: i32;
}
fun main() {
    var structArray: [x: 5];
    var structcopy = structArray[0];

    var x: [[i32: 500]: 500];
    var y: [[i32: 500]: 500];

    var res: [[i32: 500]: 500];

    for(var i = 0; i < 500; i = i + 1){
        for(var j = 0; j < 500; j = j + 1){
            x[i][j] = i;
            y[i][j] = j;
        }
    }

    for(var xie = 0; xie < 50; xie = xie + 1){
    for(var i = 0; i < 500; i = i + 1){
        for(var j = 0; j < 500; j = j + 1){
            res[i][j] = 0;
            for (var k = 0; k < 500; k = k + 1){
                res[i][j] = res[i][j] + x[i][k] * y[k][j];
            }
        }
    }
    }
    printInt(res[0][2]);

#    setSeed(2);
    for(var i: i64 = 0; i < 20; i = i + 1)
        var x = 2;
    var prev = 4;
    for(var i = 0; i < 100; i = i + 1){
        prev = randomI32(prev);
        printInt(prev);
    }
    printInt(getUnixTime());
}
