import io;

struct testStruct{
    var i: i32;
    var j: i32;
}

fun test(test: char): char{
    return test;
}
fun main(){
    var test: testStruct;
    var iderh = test.i;
    var x = 0 + 3;
    if(x == 3){
        x = 2;
    } else {
        x = 0;
    }
    var ie = &x;
    var xrie = *ie;
    var iehor = ' ';
    test(iehor);
    var h: [i32: 5];
    printInt(xrie);
    h[xrie] = 20;
    var xie = h[xrie];
    h[xie] = xie;
    printInt(xie);
    printInt(x);
}